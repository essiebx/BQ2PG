# Check current docker.env
echo "Current docker.env:"
cat docker.env

# Create a clean version
cat > docker.env << 'EOF'
GOOGLE_CLOUD_PROJECT=just-landing-398407
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/key.json
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=patents_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
DEFAULT_CHUNK_SIZE=50000
MAX_ROWS_PER_RUN=1000000
DEFAULT_SAMPLE_SIZE=10000
LOG_LEVEL=INFO
EOF

echo "✅ Clean docker.env created"